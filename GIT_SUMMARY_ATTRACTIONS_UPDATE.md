# Git Summary - Attractions Page Grouped Sections Update

## What changed
- Rebuilt `attractions.html` into a grouped attractions hub.
- Added separate sections for:
  - Places to Eat
  - Malls & Shopping
  - Walks & Nature
  - Water Attractions
  - Family Activities
- Reused the existing card pattern to practise modern card layouts, CSS Grid, grouped page structure, and responsive sections.
- Kept existing project styling and navigation.
- Used existing project images where available and emoji placeholders where Deacon can later add real photos.

## Files changed
- Modified: `attractions.html`

## Suggested commit message
```bash
git add attractions.html
git commit -m "Add grouped attractions sections"
git push
```
